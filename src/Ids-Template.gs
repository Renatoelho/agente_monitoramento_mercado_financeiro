/**
 * Ids-Template.gs
 * MODELO — copie para `src/Ids.gs` (gitignored) e cole o ID da SUA planilha.
 * Este arquivo (Ids-Template.gs) NÃO é enviado pelo clasp (ver .claspignore).
 *
 * ID da planilha = trecho da URL entre /d/ e /edit:
 *   https://docs.google.com/spreadsheets/d/<ID_DA_PLANILHA>/edit
 *
 * (O ID do Apps Script NÃO fica aqui: ele vai no `.clasp.json`, campo scriptId.)
 */
const SPREADSHEET_ID = 'COLE_AQUI_O_ID_DA_PLANILHA';
